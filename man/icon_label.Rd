% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icon.R
\name{icon_label}
\alias{icon_label}
\title{Identify an icon by its library accessor label}
\usage{
icon_label(x)
}
\arguments{
\item{x}{An \code{icon} or \code{icon_vec} object.}
}
\value{
A character vector the same length as \code{x} giving each icon's
\code{library$sub$name} label. Icons not sourced from an installed library
(a local \code{\link[=icon_set]{icon_set()}} or a \code{\link[=read_icon]{read_icon()}} file outside the icon cache)
return \code{NA}.
}
\description{
Every icon sourced from an installed icon library (as opposed to a local
\code{\link[=icon_set]{icon_set()}} or a raw \code{\link[=read_icon]{read_icon()}} file) has a file path of the form
\verb{<cache root>/<library>/<...name parts...>.svg} (see \verb{00_registry.R}'s
\code{icon_fn$get()}), which is exactly the \code{library$sub$name} expression used
to access it. \code{icon_label()} recovers that label straight from
\code{\link[=icon_path]{icon_path()}} rather than requiring a name to be tracked separately —
so, unlike \code{names()}, it survives \code{c()}/\code{rep()}/subsetting on an
\code{icon_vec} (a \code{vctrs} rcrd, which cannot carry element \code{names()}).
}
